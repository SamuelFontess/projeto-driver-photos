import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Redefinir senha — Projeto Driver',
};

export default function ResetPasswordLayout({ children }: { children: React.ReactNode }) {
  return children;
}
