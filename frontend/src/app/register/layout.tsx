import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Criar conta — Projeto Driver',
};

export default function RegisterLayout({ children }: { children: React.ReactNode }) {
  return children;
}
